[[rulesets]]
[rulesets.rules]
"{items}" = "{items}"
"{materials}" = "{materials}"
"{plants}" = "{plants}"
"{skills}" = "{skills}"
"{tasks}" = "{tasks}"
"{tiles}" = "{tiles}"
"{day}" = "{day}"
"{menu}" = "{menu}"
"{require}" = "{require}"
"{thought}" = "{thought}"
"{memory}" = "{memory}"
"{health}" = "{health}"